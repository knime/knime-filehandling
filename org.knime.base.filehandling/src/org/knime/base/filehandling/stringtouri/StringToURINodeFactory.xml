<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE knimeNode PUBLIC "-//UNIKN//DTD KNIME Node 2.0//EN" "http://www.knime.org/Node.dtd">
<knimeNode icon="./stringtouri.png" type="Manipulator">
	<name>String To URI</name>

	<shortDescription>
		Convert a string column into an URI column.
	</shortDescription>

	<fullDescription>
		<intro>
			This node converts the selected string column into a column of
			the type URI.
		</intro>


		<option name="Column selection">Column that will be converted. It has to
			contain a
			string with the correct URI syntax.
		</option>
		<option name="String represents...">
			<b>File path</b>
			<br />
			Input string represents a file path (e.g. "c:\temp\file.dat").
			<br />
			<b>URI</b>
			<br />
			Input string represents an URI (e.g. "file:/c:/temp/file.dat").
		</option>
		<option name="Fail if file does not exist (only applies to local files)">Checks if the files referenced by the created URIs
			are existing and will abort if one is not.
		</option>
		<option name="Append column">Does append the URI column to the table instead of
			replacing the selected column.
		</option>
		<option name="Appended column name">Name of the appended column</option>
	</fullDescription>

	<ports>
		<inPort name="Input table" index="0">
			Table that contains a string
			column with URI information.
		</inPort>
		<outPort name="Output table" index="0">
			Input table with the
			corresponding URI column to the selected column. It will either be
			appended or replacing the old column, depending on the configuration.
		</outPort>
	</ports>
</knimeNode>
