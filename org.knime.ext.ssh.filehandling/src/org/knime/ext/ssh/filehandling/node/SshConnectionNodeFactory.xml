<?xml version="1.0" encoding="UTF-8"?>
<knimeNode icon="./sshconnection16x16.png"
		type="Source"
		xmlns="http://knime.org/node/v4.1"
		xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		xsi:schemaLocation="http://knime.org/node/v4.1 http://knime.org/node/v4.1.xsd">
    <name>SSH Connector (Labs)</name>
    
    <shortDescription>
        Connects to remote file system by SSH in order to read/write files in downstream nodes.
    </shortDescription>
    
    <fullDescription>
         <intro>
            <p>This node connects to remote file system by SSH. The resulting output port allows downstream nodes to access
            the <i>files</i> of the remote file system, e.g. to read or write,
            or to perform other file system operations (browse/list files, copy, move, ...).
            </p>
            
            <p><b>Path syntax:</b> Paths for SSH are specified with a UNIX-like syntax, /myfolder/myfile. An absolute
            for SSH consists of:
                <ol>
                    <li>A leading slash ("/").</li>
                    <li>Followed by the path to the file ("myfolder/myfile" in the above example).</li>
                </ol>
            </p>
        </intro>
        
        <tab name="Settings">
	        <option name="Host">
	            SSH remote host.
	        </option>
	        <option name="Port">
	            SSH remote port.
	        </option>
	        <option name="Authentication">
	        	Authentication
	            <ul>
	            	<li>
	            		<i>Credentials:</i> workflow credentials authentication.
	            	</li>
	            	<li>
	            		<i>Username &amp; password:</i> user name and password authentication.
	            	</li>
	            	<li>
	            		<i>Key file:</i> private key file authentication.
	            	</li>
	           	</ul>
	        </option>
		    <option name="Working directory">
		        Specify the <i>working directory</i> of the resulting file system connection, using the <b>Path syntax</b> explained above. The working
		        directory must be specified as an absolute path. A working directory allows downstream nodes to access files/folders using <i>relative</i>
		        paths, i.e. paths that do not have a leading slash. The default working directory is the root "/", under which all the
		        document libraries are located.
		    </option>
	    </tab>
	    
        <tab name="Advanced">
		    <option name="Connection timeout in seconds">
		        Timeout in seconds to establish a connection or 0 for an infinite timeout. 
		    </option>
		    <option name="Use known hosts">
		        If this options selected the known hosts file will used for verify remote hosts.
		    </option>
		</tab>
    </fullDescription>

    <ports>
		<dynInPort insert-before="0" name="Input File System" group-identifier="Input File System"> 
			File system with SSH key and known_hosts file 
		</dynInPort>
   		<outPort index="0" name="SSH File System Connection">
   			SSH File System Connection.
   		</outPort>
    </ports>    
</knimeNode>
